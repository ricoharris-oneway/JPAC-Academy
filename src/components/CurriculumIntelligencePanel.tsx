import{useState}from'react';import{supabase}from'../lib/supabase';import{AriaKnowledgeLibrary}from'./AriaKnowledgeLibrary';import'../styles/grounded-intelligence.css';
type Action='improve_lesson'|'regenerate_activity'|'modernize_module'|'analyze_course'|'rebuild_course';type RecordLike=Record<string,unknown>;
type SourceSupport={sourceSectionId:string|null;sourceTitle:string;sourceVersion:string|null;sectionHeading:string;sectionKey:string|null;classification:string;usedFor:string[];reasonUsed:string;excerpt:string|null;relevance?:{score:number;matchedTerms:string[];method:string}|null};
type Proposal={scope:string;changeType:string;current:RecordLike|null;proposed:RecordLike;changes:Array<{section:string;decision:string;summary:string;sourceSectionIds?:string[]}>;rationale:string;validation:RecordLike;sourceSupport?:SourceSupport[];conflicts?:Array<{conflictType?:string;sourceA?:string;sourceB?:string;canonicalField?:string;explanation?:string;recommendedInterpretation:string}>;confidence?:number};
type SourceSummary={id:string;title:string;version:string;classification:string};type Response={provider:string;providerMode:'live'|'development_mock';model:string;lifecycle:string;label:string;contextSummary:{canonicalLesson:boolean;moduleContext:boolean;stagedModules:number;stagedLessons:number;stagedActivities:number;relevantSourceSections:number;sources:SourceSummary[]};proposal:Proposal};
type Props={role:string;course?:{id:string;title:string;slug:string};module?:{id:string;title:string;status:string;level_module_number:number};lesson?:{id:string;title:string;status:string};activity?:{id:string;title:string;status:string}};
const label=(key:string)=>key.replaceAll('_',' ').replace(/\b\w/g,value=>value.toUpperCase());
const display=(value:unknown):string=>Array.isArray(value)?value.map(item=>typeof item==='object'?Object.values(item as RecordLike).filter(Boolean).join(' — '):String(item)).join('\n'):value&&typeof value==='object'?Object.entries(value as RecordLike).map(([key,item])=>`${label(key)}: ${display(item)}`).join('\n'):String(value??'Not configured');
const comparableKeys=['title','learning_objective','instructional_content','content_blocks','practice','aria_evidence_targets','jpac_lab_integration','career_connection','assessment_relationship','description','instructions','xp_type','xp_reward','passing_score','submission_type','rubric','plan','levels','strengthScore','health','strengths','gaps','modernizationOpportunities','administratorGoal'];

function Comparison({proposal}:{proposal:Proposal}){const current=proposal.current||{},proposed=proposal.proposed||{};const keys=comparableKeys.filter(key=>key in current||key in proposed);return <div className="ci-comparison">{keys.map(key=>{const before=display(current[key]),after=display(proposed[key]);const state=!(key in current)?'added':!(key in proposed)?'removed':before===after?'unchanged':'modified';return <article key={key} className={`ci-diff ${state}`}><header><b>{label(key)}</b><span>{state}</span></header><div><section><small>Current</small><p>{before}</p></section><section><small>Proposed</small><p>{after}</p></section></div></article>})}</div>}
function SourceSupportView({sources}:{sources:SourceSupport[]}){return <section className="ci-sources"><h3>Source support</h3>{sources.map((source,index)=><article key={`${source.sourceSectionId||'unsupported'}-${index}`} className={source.sourceSectionId?'':'unsupported'}><header><div><b>{source.sourceTitle}{source.sourceVersion?` · v${source.sourceVersion}`:''}</b><span>Section: {source.sectionHeading}</span></div>{source.classification&&<small>{label(source.classification)}</small>}</header><div className="ci-used-for"><strong>Used for</strong>{source.usedFor.map(item=><span key={item}>{item}</span>)}</div><p>{source.reasonUsed}</p>{source.excerpt&&<details><summary>Inspect supporting excerpt</summary><blockquote>{source.excerpt}</blockquote><small>Section key: {source.sectionKey} · relevance score {source.relevance?.score??'n/a'}</small></details>}</article>)}</section>}

export function CurriculumIntelligencePanel({role,course,module,lesson,activity}:Props){
 const[result,setResult]=useState<Response|null>(null);const[busy,setBusy]=useState<Action|null>(null);const[message,setMessage]=useState('');const[administratorGoal,setAdministratorGoal]=useState('');if(!['admin','developer'].includes(role))return null;
 async function generate(action:Action){if(!course)return;setBusy(action);setMessage('');setResult(null);const{data}=await supabase!.auth.getSession();const response=await fetch('/api/curriculum-intelligence',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${data.session?.access_token||''}`},body:JSON.stringify({action,courseId:course.id,moduleId:module?.id,lessonId:lesson?.id,activityId:activity?.id,administratorInstruction:administratorGoal})});const payload=await response.json();setBusy(null);if(!response.ok){setMessage(payload.error||'Unable to generate curriculum proposal.');return}setResult(payload as Response)}
 const moduleOneMissingReviewedChallenge=course?.slug==='singing'&&module?.level_module_number===1&&activity?.title!=='Breath Control Studio Challenge';
 const moduleOne=moduleOneMissingReviewedChallenge;
 const stagedCount=result?result.contextSummary.stagedModules+result.contextSummary.stagedLessons+result.contextSummary.stagedActivities:0;return <section className="card ci-panel"><div className="section-head"><div><div className="eyebrow">Administrative proposal workspace</div><h2>Curriculum Intelligence</h2><p>Analyze canonical curriculum and prepare reviewable drafts. Nothing here publishes or changes student records.</p></div><span className="ci-safety">PROPOSAL ONLY</span></div>{moduleOne&&<div className="ci-known-issue"><b>Known Module 1 state</b><span>The reviewed E3 Core Challenge is missing. Analysis retains the published legacy challenge and will not manufacture or publish the missing record.</span></div>}<AriaKnowledgeLibrary courseId={course?.id} discipline={course?.title}/><label className="ci-goal">Administrator goal<textarea value={administratorGoal} onChange={event=>setAdministratorGoal(event.target.value)} placeholder="Optional: describe the intended improvement, audience, standards, or levels to analyze."/></label><div className="ci-actions"><button disabled={!lesson||Boolean(busy)} onClick={()=>void generate('improve_lesson')}><b>Improve Lesson</b><span>{lesson?.title||'Select a lesson'}</span></button><button disabled={!activity||Boolean(busy)} onClick={()=>void generate('regenerate_activity')}><b>Regenerate Activity</b><span>{activity?.title||'Select an activity'}</span></button><button disabled={!module||Boolean(busy)} onClick={()=>void generate('modernize_module')}><b>Modernize Module</b><span>{module?.title||'Select a module'}</span></button><button disabled={!course||Boolean(busy)} onClick={()=>void generate('analyze_course')}><b>Analyze Course</b><span>{course?.title||'Select a course'}</span></button><button disabled={!course||Boolean(busy)} onClick={()=>void generate('rebuild_course')}><b>Full Course Rebuild</b><span>Generate review-only course version proposal</span></button></div>{busy&&<div className="ci-loading">Assembling canonical, staged, trusted-source, and safety context…</div>}{message&&<div className="admin-message">{message}</div>}{result&&<div className="ci-result"><div className="ci-result-head"><div><span>{result.label}</span><h3>{label(result.proposal.scope)} proposal</h3></div><small>{result.provider} · {result.model} · {Math.round((result.proposal.confidence||0)*100)}% confidence</small></div>{result.providerMode==='development_mock'&&<div className="ci-provider-mode"><b>DEVELOPMENT / MOCK MODE</b><span>No configured LLM generated this proposal. Enable an approved server-side provider for generative grounded reasoning.</span></div>}<section className="ci-analyzed"><h3>ARIA analyzed</h3><span>{result.contextSummary.canonicalLesson?'Canonical lesson':'Canonical course'}</span><span>{result.contextSummary.moduleContext?'Module context':'Course context'}</span>{result.contextSummary.sources.map(source=><span key={source.id}>{source.title} v{source.version}</span>)}<strong>{result.contextSummary.relevantSourceSections} relevant trusted section{result.contextSummary.relevantSourceSections===1?'':'s'}</strong>{Boolean(stagedCount)&&<span>{stagedCount} staged curriculum item{stagedCount===1?'':'s'}</span>}</section><Comparison proposal={result.proposal}/><section className="ci-rationale"><div className="eyebrow">Why ARIA recommends this</div><p>{result.proposal.rationale}</p></section><SourceSupportView sources={result.proposal.sourceSupport||[]}/>{Boolean(result.proposal.conflicts?.length)&&<section className="ci-conflicts"><h3>Authority conflict · administrator review required</h3>{result.proposal.conflicts!.map((conflict,index)=><article key={index}><span>{conflict.conflictType||'SOURCE CONFLICT'}</span>{conflict.canonicalField&&<p><b>Canonical field:</b> {conflict.canonicalField}</p>}{conflict.sourceA&&<p><b>Source A:</b> {conflict.sourceA}</p>}{conflict.sourceB&&<p><b>Source B:</b> {conflict.sourceB}</p>}{conflict.explanation&&<p>{conflict.explanation}</p>}<p><b>Recommended interpretation:</b> {conflict.recommendedInterpretation}</p></article>)}</section>}<div className="ci-change-list"><h3>Change plan</h3>{result.proposal.changes.length?result.proposal.changes.map((change,index)=><article key={`${change.section}-${index}`}><span>{change.decision}</span><div><b>{change.section}</b><p>{change.summary}</p>{Boolean(change.sourceSectionIds?.length)&&<small>Supported by {change.sourceSectionIds!.length} cited section{change.sourceSectionIds!.length===1?'':'s'}</small>}</div></article>):<p>Analysis only — no curriculum change set was generated.</p>}</div><footer><span>Lifecycle: GENERATED</span><b>Review required · approval would not publish</b></footer></div>}</section>
}

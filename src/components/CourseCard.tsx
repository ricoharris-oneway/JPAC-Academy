import{Link}from'react-router-dom';import type{Course}from'../types';import{ProgressBar}from'./ui';export function CourseCard({course}:{course:Course}){return <article className="card course-card"><div className="course-art">{course.icon}</div><div className="course-card-content"><div className="eyebrow">{course.category} · {course.totalXp.toLocaleString()} XP</div><h3>{course.title}</h3><p className="muted">{course.currentModule}</p><ProgressBar value={course.progress}/><div className="course-actions"><small>{course.progress}% complete</small><Link className="button button-primary" to="/studio">{course.progress?'Continue':'Explore'}</Link></div></div></article>}

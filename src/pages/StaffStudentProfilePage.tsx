import{useEffect,useMemo,useState}from'react';import{Link,useParams}from'react-router-dom';import{supabase}from'../lib/supabase';
type Profile={id:string;display_name:string|null;email:string|null;first_name:string|null;last_name:string|null;total_xp:number;created_at:string};
type StudentProfile={guardian_name:string|null;guardian_email:string|null;guardian_phone:string|null;birth_date:string|null;school_name:string|null;grade_level:string|null;primary_goal:string|null;onboarding_complete:boolean;practice_streak:number;last_practice_date:string|null};
type Pending={id:string;admissions_stage:string;enrollment_status:string;invitation_status:string;student_type:string;academy_experience:string;scholarship_status:string|null;scholarship_type:string|null;transportation_needed:string|null;transportation_pickup:string|null;authorized_pickup_names:string|null;secondary_program:string|null;experience_level:string|null;practice_availability:string|null;medical_accessibility_notes:string|null;photo_release_consent:boolean|null;digital_communication_consent:boolean|null;activated_at:string|null;accepted_at:string|null;course:{title:string}|{title:string}[]|null};
type Enrollment={id:string;status:string;progress:number;level:number;start_date:string|null;target_completion_date:string|null;course:{title:string}|{title:string}[]|null;teacher:{display_name:string}|{display_name:string}[]|null};
type Payment={id:string;payment_status:string;payment_method:string;amount:number|null;currency:string;payment_date:string;course:{title:string}|{title:string}[]|null};
type Consent={consent_status:string;guardian_name:string|null;guardian_email:string|null;signed_at:string|null;promotional_media_consent:boolean;internal_learning_media_consent:boolean};
type Timeline={id:string;event_type:string;title:string;description:string;occurred_at:string};
const one=<T,>(x:T|T[]|null|undefined)=>Array.isArray(x)?x[0]:x||null;
export function StaffStudentProfilePage(){const{studentId=''}=useParams();const[profile,setProfile]=useState<Profile|null>(null);const[studentProfile,setStudentProfile]=useState<StudentProfile|null>(null);const[pending,setPending]=useState<Pending|null>(null);const[enrollments,setEnrollments]=useState<Enrollment[]>([]);const[payments,setPayments]=useState<Payment[]>([]);const[consent,setConsent]=useState<Consent|null>(null);const[timeline,setTimeline]=useState<Timeline[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');
useEffect(()=>{let active=true;void(async()=>{if(!supabase)return;setLoading(true);const r=await Promise.all([supabase.from('profiles').select('id,display_name,email,first_name,last_name,total_xp,created_at').eq('id',studentId).eq('role','student').maybeSingle(),supabase.from('student_profiles').select('guardian_name,guardian_email,guardian_phone,birth_date,school_name,grade_level,primary_goal,onboarding_complete,practice_streak,last_practice_date').eq('user_id',studentId).maybeSingle(),supabase.from('pending_students').select('id,admissions_stage,enrollment_status,invitation_status,student_type,academy_experience,scholarship_status,scholarship_type,transportation_needed,transportation_pickup,authorized_pickup_names,secondary_program,experience_level,practice_availability,medical_accessibility_notes,photo_release_consent,digital_communication_consent,activated_at,accepted_at,course:courses(title)').eq('linked_profile_id',studentId).maybeSingle(),supabase.from('enrollments').select('id,status,progress,level,start_date,target_completion_date,course:courses(title),teacher:profiles!enrollments_teacher_id_fkey(display_name)').eq('student_id',studentId).order('enrolled_at',{ascending:false}),supabase.from('student_payment_ledger').select('id,payment_status,payment_method,amount,currency,payment_date,course:courses(title)').eq('student_id',studentId).order('payment_date',{ascending:false}),supabase.from('student_consent_ledger').select('consent_status,guardian_name,guardian_email,signed_at,promotional_media_consent,internal_learning_media_consent').eq('student_id',studentId).maybeSingle(),supabase.from('student_timeline').select('id,event_type,title,description,occurred_at').eq('student_id',studentId).order('occurred_at',{ascending:false}).limit(12)]);if(!active)return;setProfile(r[0].data as Profile|null);setStudentProfile(r[1].data as StudentProfile|null);setPending(r[2].data as Pending|null);setEnrollments((r[3].data as Enrollment[])||[]);setPayments((r[4].data as Payment[])||[]);setConsent(r[5].data as Consent|null);setTimeline((r[6].data as Timeline[])||[]);setError(r.find(x=>x.error)?.error?.message||'');setLoading(false)})();return()=>{active=false}},[studentId]);
useEffect(()=>{if(profile?.email){localStorage.setItem('jpac_active_student_email',profile.email);localStorage.setItem('jpac_active_student_id',profile.id)}},[profile]);
const active=enrollments.filter(e=>e.status==='active');const progress=active.length?Math.round(active.reduce((s,e)=>s+Number(e.progress||0),0)/active.length):0;const status=pending?.admissions_stage||'profile';const guardian=studentProfile?.guardian_name||consent?.guardian_name||'Not recorded';
if(loading)return <div className="card card-pad"><h2>Loading student profile…</h2></div>;if(!profile)return <div className="card card-pad"><h2>Student not found</h2><p>{error||'No JPAC student profile was found.'}</p></div>;
return <><div className="page-hero"><div><div className="eyebrow">Staff student record</div><h1 className="page-title">{profile.display_name||'JPAC Student'}</h1><p className="muted">One place for admissions, enrollment, guardian, consent, payment, progress and student support information.</p></div><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><Link className="button button-secondary" to={`/staff/course-enrollment?student=${encodeURIComponent(profile.email||'')}`}>Enrollment</Link><Link className="button button-secondary" to={`/staff/payment-ledger?student=${encodeURIComponent(profile.email||'')}`}>Payments</Link><Link className="button button-secondary" to={`/staff/consent-ledger?student=${encodeURIComponent(profile.email||'')}`}>Consent</Link></div></div>{error&&<div className="admin-message error">{error}</div>}<div className="ops-kpis"><article><span>🎓</span><strong>{status.replaceAll('_',' ')}</strong><small>Pipeline status</small></article><article><span>📚</span><strong>{active.length}</strong><small>Active courses</small></article><article><span>📈</span><strong>{progress}%</strong><small>Average progress</small></article><article><span>✨</span><strong>{Number(profile.total_xp||0).toLocaleString()}</strong><small>Total XP</small></article></div><div className="ops-grid"><section className="card ops-panel"><div className="eyebrow">Identity & contact</div><h2>Student information</h2><p><strong>Email:</strong> {profile.email||'Not recorded'}</p><p><strong>Date of birth:</strong> {studentProfile?.birth_date||'Not recorded'}</p><p><strong>School:</strong> {studentProfile?.school_name||'Not recorded'}</p><p><strong>Grade:</strong> {studentProfile?.grade_level||'Not recorded'}</p><p><strong>Experience:</strong> {pending?.experience_level||'Not recorded'}</p><p><strong>Primary goal:</strong> {studentProfile?.primary_goal||'Not recorded'}</p></section><section className="card ops-panel"><div className="eyebrow">Admissions & onboarding</div><h2>Pipeline</h2><p><strong>Admissions stage:</strong> {pending?.admissions_stage||'No admissions record'}</p><p><strong>Enrollment status:</strong> {pending?.enrollment_status||'No admissions record'}</p><p><strong>Login invitation:</strong> {pending?.invitation_status||'Not recorded'}</p><p><strong>Academy experience:</strong> {pending?.academy_experience||'Not recorded'}</p><p><strong>Student type:</strong> {pending?.student_type||'Not recorded'}</p><p><strong>Onboarding complete:</strong> {studentProfile?.onboarding_complete?'Yes':'No'}</p></section></div><div className="ops-grid"><section className="card ops-panel"><div className="eyebrow">Guardian & support</div><h2>Family and student support</h2><p><strong>Guardian:</strong> {guardian}</p><p><strong>Guardian email:</strong> {studentProfile?.guardian_email||consent?.guardian_email||'Not recorded'}</p><p><strong>Guardian phone:</strong> {studentProfile?.guardian_phone||'Not recorded'}</p><p><strong>Transportation:</strong> {pending?.transportation_needed||'Not requested'}</p><p><strong>Pickup location:</strong> {pending?.transportation_pickup||'Not recorded'}</p><p><strong>Authorized pickup:</strong> {pending?.authorized_pickup_names||'Not recorded'}</p><p><strong>Accessibility/support notes:</strong> {pending?.medical_accessibility_notes||'None recorded'}</p></section><section className="card ops-panel"><div className="eyebrow">Scholarship & consent</div><h2>Access documentation</h2><p><strong>Scholarship:</strong> {pending?.scholarship_status||'Not Requested'}{pending?.scholarship_type?` · ${pending.scholarship_type}`:''}</p><p><strong>Consent status:</strong> {consent?.consent_status||'No consent record'}</p><p><strong>Signed:</strong> {consent?.signed_at?new Date(consent.signed_at).toLocaleString():'Not signed'}</p><p><strong>Photo/video release:</strong> {pending?.photo_release_consent===true?'Yes':pending?.photo_release_consent===false?'No':'Not recorded'}</p><p><strong>Digital communication:</strong> {pending?.digital_communication_consent===true?'Yes':pending?.digital_communication_consent===false?'No':'Not recorded'}</p></section></div><section className="card ops-panel"><div className="section-head"><div><div className="eyebrow">Academic record</div><h2>Course enrollments</h2></div></div>{enrollments.length? <div className="ops-feed">{enrollments.map(e=><article key={e.id}><span>🎓</span><div><strong>{one(e.course)?.title||'JPAC Course'} · Level {e.level||1}</strong><small>{e.status} · {Math.round(Number(e.progress||0))}% complete{one(e.teacher)?.display_name?` · ${one(e.teacher)?.display_name}`:''}</small></div></article>)}</div>:<p className="muted">No course enrollments found.</p>}</section><div className="ops-grid"><section className="card ops-panel"><div className="eyebrow">Payment history</div><h2>Ledger</h2>{payments.length?payments.slice(0,6).map(p=><p key={p.id}><strong>{one(p.course)?.title||'Unlinked course'}:</strong> {p.payment_status} · {p.amount===null?'Amount not recorded':`${p.currency} ${Number(p.amount).toFixed(2)}`} · {p.payment_date}</p>):<p className="muted">No payment ledger records.</p>}</section><section className="card ops-panel"><div className="eyebrow">Student timeline</div><h2>Recent activity</h2>{timeline.length?<div className="ops-feed">{timeline.map(t=><article key={t.id}><span>•</span><div><strong>{t.title}</strong><small>{t.description}</small></div><time>{new Date(t.occurred_at).toLocaleDateString()}</time></article>)}</div>:<p className="muted">No timeline activity yet.</p>}</section></div></>}

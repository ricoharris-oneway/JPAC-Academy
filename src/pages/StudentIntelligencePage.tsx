import{useEffect,useMemo,useState}from'react';import{Link}from'react-router-dom';import{supabase}from'../lib/supabase';import{useAuth}from'../context/AuthContext';import{WorkspaceHero}from'../components/WorkspaceHero';import{ProgressRing,Timeline,EmptyState,SkeletonCards}from'../components/CreativeComponents';
type Student={id:string;display_name:string;email:string|null;total_xp:number};type Twin={student_id:string;creative_health:number;course_progress_score:number;practice_consistency:number;confidence_score:number;technique_score:number;creativity_score:number;professionalism_score:number;portfolio_score:number;goal_progress_score:number;performance_readiness:number;career_readiness:number;learning_velocity:number;risk_level:string;trend:string;summary:string;next_best_action:string;calculated_at:string};type Preference={student_id:string;visual_score:number;auditory_score:number;kinesthetic_score:number;reading_score:number;preferred_pace:string;preferred_feedback:string};type Strength={id:string;student_id:string;skill_name:string;confidence:number};type Recommendation={id:string;student_id:string;title:string;rationale:string;priority:number;status:string;recommendation_type:string};type JourneyEvent={id:string;student_id:string;event_type:string;title:string;description:string;occurred_at:string};
const blankTwin:Omit<Twin,'student_id'|'calculated_at'>={creative_health:50,course_progress_score:0,practice_consistency:50,confidence_score:50,technique_score:50,creativity_score:50,professionalism_score:50,portfolio_score:0,goal_progress_score:0,performance_readiness:0,career_readiness:0,learning_velocity:0,risk_level:'low',trend:'stable',summary:'ARIA is waiting for learning evidence.',next_best_action:'Complete a Wix lesson and a focused JPAC LAB activity.'};
export function StudentIntelligencePage(){const{user,profile}=useAuth();const[students,setStudents]=useState<Student[]>([]);const[twins,setTwins]=useState<Twin[]>([]);const[preferences,setPreferences]=useState<Preference[]>([]);const[strengths,setStrengths]=useState<Strength[]>([]);const[recommendations,setRecommendations]=useState<Recommendation[]>([]);const[timeline,setTimeline]=useState<JourneyEvent[]>([]);const[selected,setSelected]=useState('');const[message,setMessage]=useState('');const[loading,setLoading]=useState(true);const isStaff=Boolean(profile&&['teacher','admin','developer'].includes(profile.role));
async function load(){if(!supabase||!user)return;setLoading(true);const studentFilter=isStaff?supabase.from('profiles').select('id,display_name,email,total_xp').eq('role','student').order('display_name'):supabase.from('profiles').select('id,display_name,email,total_xp').eq('id',user.id);const[{data:s},{data:t},{data:p},{data:st},{data:r},{data:tl}]=await Promise.all([studentFilter,supabase.from('student_digital_twins').select('*'),supabase.from('student_learning_preferences').select('student_id,visual_score,auditory_score,kinesthetic_score,reading_score,preferred_pace,preferred_feedback'),supabase.from('student_strengths').select('id,student_id,skill_name,confidence').order('confidence',{ascending:false}),supabase.from('aria_recommendations').select('id,student_id,title,rationale,priority,status,recommendation_type').eq('status','active').order('priority',{ascending:false}),supabase.from('student_timeline').select('id,student_id,event_type,title,description,occurred_at').order('occurred_at',{ascending:false}).limit(60)]);const studentRows=(s as Student[])||[];setStudents(studentRows);setTwins((t as Twin[])||[]);setPreferences((p as Preference[])||[]);setStrengths((st as Strength[])||[]);setRecommendations((r as Recommendation[])||[]);setTimeline((tl as JourneyEvent[])||[]);if(!selected&&studentRows[0])setSelected(studentRows[0].id);setLoading(false)}useEffect(()=>{void load()},[user?.id,profile?.role]);
const student=students.find(x=>x.id===selected);const twin=twins.find(x=>x.student_id===selected)||({...blankTwin,student_id:selected,calculated_at:''}as Twin);const pref=preferences.find(x=>x.student_id===selected);const studentStrengths=strengths.filter(x=>x.student_id===selected).slice(0,6);const studentRecommendations=recommendations.filter(x=>x.student_id===selected).slice(0,4);const studentTimeline=timeline.filter(x=>x.student_id===selected).slice(0,8);const dimensions=useMemo(()=>[['Course progress',twin.course_progress_score],['Creative flame',twin.practice_consistency],['Confidence',twin.confidence_score],['Technique',twin.technique_score],['Creativity',twin.creativity_score],['Professionalism',twin.professionalism_score],['Portfolio',twin.portfolio_score],['Career readiness',twin.career_readiness]] as [string,number][],[twin]);
async function refreshTwin(){if(!supabase||!selected)return;setMessage('ARIA is refreshing this creative journey…');const{error}=await supabase.rpc('refresh_student_digital_twin',{target_student:selected,refresh_reason:'My Creative Journey refresh'});setMessage(error?.message||'Creative Journey refreshed from current Academy evidence.');if(!error)await load()}
if(loading)return <SkeletonCards count={5}/>;if(!student)return <EmptyState icon="🌟" title="No creative journey available yet" detail="Create or enroll a student before ARIA begins building a Digital Twin." actionLabel="Open Admissions" actionTo="/manual-student"/>;
const mission=studentRecommendations[0];const firstName=student.display_name.split(' ')[0];return <div className="creative-journey"><WorkspaceHero eyebrow={isStaff?'Student Intelligence · Creative Journey':'My Growth · Creative Journey'} title={isStaff?`${student.display_name}'s Creative Journey`:`Welcome back, ${firstName}.`} description="A living story of growth, confidence, skill, practice, creativity, and the next step toward greatness." environment="student" ariaLabel="ARIA is walking beside you" ariaMessage={twin.summary||'Every lesson, practice session, and project adds another chapter to this journey.'} actions={<><button className="button button-primary" onClick={()=>void refreshTwin()}>Refresh journey</button><Link className="button button-secondary" to="/studio">Continue creating</Link></>} stats={[{icon:'✨',value:student.total_xp.toLocaleString(),label:'Total XP'},{icon:'🚀',value:`${Math.round(twin.performance_readiness)}%`,label:'Performance ready'},{icon:'💼',value:`${Math.round(twin.career_readiness)}%`,label:'Career ready'}]}><ProgressRing value={twin.creative_health} label="Creative Health" size={132}/></WorkspaceHero>{message&&<div className="admin-message">{message}</div>}{isStaff&&<div className="journey-selector">{students.map(s=><button key={s.id} className={selected===s.id?'active':''} onClick={()=>setSelected(s.id)}><span>{s.display_name.split(' ').map(x=>x[0]).join('').slice(0,2)}</span><strong>{s.display_name}</strong></button>)}</div>}<section className="card journey-profile"><div className="journey-identity"><span className="journey-avatar">{student.display_name.split(' ').map(x=>x[0]).join('').slice(0,2)}</span><div><div className="eyebrow">Artist profile</div><h2>{student.display_name}</h2><p>{student.email||'JPAC creator'} · {twin.trend} momentum</p></div></div><ProgressRing value={twin.creative_health} label="Creative Health" size={112}/><span className={`journey-risk ${twin.risk_level}`}>{twin.risk_level} support need</span></section><div className="journey-primary"><section className="card journey-section"><div className="eyebrow">Progress toward greatness</div><h2>Your creative development</h2><div className="journey-dimensions">{dimensions.map(([label,value])=><div className="journey-dimension" key={label}><div><strong>{label}</strong><small>{Math.round(value)}%</small></div><div className="journey-dimension-track"><i style={{width:`${Math.max(0,Math.min(100,value))}%`}}/></div></div>)}</div></section><aside className="card journey-mission"><span className="journey-mission-icon">🎯</span><div className="eyebrow">Today’s mission</div><h2>{mission?.title||twin.next_best_action}</h2><p>{mission?.rationale||'One focused creative action today keeps your momentum strong and gives ARIA better evidence for your next recommendation.'}</p><div className="journey-mission-stats"><span><b>15–20 min</b><small>Estimated focus</small></span><span><b>+250 XP</b><small>Momentum reward</small></span></div><Link className="button button-primary" to="/studio">Start today’s mission</Link></aside></div><div className="journey-lower"><section className="card journey-section"><div className="eyebrow">Learning DNA</div><h2>How you learn best</h2>{pref?<><div className="learning-bars"><span style={{height:`${Math.max(28,pref.visual_score*100)}%`}}>Visual<b>{Math.round(pref.visual_score*100)}</b></span><span style={{height:`${Math.max(28,pref.auditory_score*100)}%`}}>Audio<b>{Math.round(pref.auditory_score*100)}</b></span><span style={{height:`${Math.max(28,pref.kinesthetic_score*100)}%`}}>Hands-on<b>{Math.round(pref.kinesthetic_score*100)}</b></span><span style={{height:`${Math.max(28,pref.reading_score*100)}%`}}>Reading<b>{Math.round(pref.reading_score*100)}</b></span></div><p className="muted">Best pace: {pref.preferred_pace} · Best feedback: {pref.preferred_feedback}</p></>:<p className="muted">ARIA will shape this profile as learning evidence grows.</p>}</section><section className="card journey-section"><div className="eyebrow">Your strongest lights</div><h2>Creative strengths</h2>{studentStrengths.length?<div className="strength-chips">{studentStrengths.map(x=><span key={x.id}>{x.skill_name}<b>{Math.round(x.confidence*100)}%</b></span>)}</div>:<div className="journey-empty">Teacher observations, Wix progress, and projects will reveal strengths here.</div>}</section><section className="card journey-section"><div className="eyebrow">ARIA insights</div><h2>Next opportunities</h2><div className="journey-actions">{studentRecommendations.length?studentRecommendations.map(x=><article className="journey-action" key={x.id}><span>{x.recommendation_type}</span><strong>{x.title}</strong><p>{x.rationale}</p></article>):<article className="journey-action"><span>practice</span><strong>{twin.next_best_action}</strong><p>Initial recommendation. Personalization improves as more evidence is collected.</p></article>}</div></section></div><section className="card journey-timeline-wrap"><div className="eyebrow">Your story so far</div><h2>Creative Journey</h2>{studentTimeline.length?<Timeline items={studentTimeline.map((x,i)=>({icon:i===0?'✨':'●',title:x.title,detail:x.description,time:new Date(x.occurred_at).toLocaleDateString(),status:i===0?'current':'complete'}))}/>:<EmptyState icon="🚀" title="Your journey is beginning" detail="Enrollments, practice, certificates, performances, and portfolio milestones will build this story."/>}</section></div>}

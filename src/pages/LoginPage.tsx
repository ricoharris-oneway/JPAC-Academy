import{useEffect,useState,type FormEvent}from'react';import{Link,Navigate,useLocation,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';
export function LoginPage(){const{user,loading,signIn,signUp}=useAuth();const location=useLocation();const navigate=useNavigate();const[mode,setMode]=useState<'signin'|'signup'>('signin');const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[message,setMessage]=useState((location.state as{message?:string}|null)?.message||'');const[busy,setBusy]=useState(false);useEffect(()=>{if(user){const destination=(location.state as{from?:string}|null)?.from||'/';navigate(destination,{replace:true})}},[user,location.state,navigate]);if(!loading&&user)return <Navigate to="/" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');const error=mode==='signin'?await signIn(email,password):await signUp(email,password,name);setMessage(error||(mode==='signup'?'Account created. Check your email to confirm your account.':'Welcome back.'));setBusy(false)}return <main className="auth-page"><section className="auth-visual"><img src="/assets/jpac-official-logo.png.png" alt="J. Moné's Performing Arts Center official logo"/><div className="eyebrow">J. Moné's Performing Arts Center</div><h1>Greatness starts now.</h1><p>Enter your creative campus, continue your courses, build XP, and shape your professional path.</p></section><section className="auth-panel card"><div className="eyebrow">JPAC Academy</div><h2>{mode==='signin'?'Welcome back':'Create your academy account'}</h2><p className="muted">{mode==='signin'?'First visit? Use the activation link emailed after your Wix purchase to set your password.':'New accounts begin as students until an administrator assigns another role.'}</p><form onSubmit={submit} className="auth-form">{mode==='signup'&&<label>Full name<input required value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==='signin'?'current-password':'new-password'}/></label><button className="button button-primary" disabled={busy}>{busy?'Please wait…':mode==='signin'?'Enter Academy':'Create Account'}</button></form>{mode==='signin'&&<Link className="auth-switch" to="/forgot-password">Forgot password?</Link>}{message&&<div className="auth-message" role="status">{message}</div>}<button className="auth-switch" onClick={()=>{setMode(mode==='signin'?'signup':'signin');setMessage('')}}>{mode==='signin'?'Need an account? Create one':'Already registered? Sign in'}</button></section></main>}

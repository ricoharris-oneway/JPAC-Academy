import{useMemo,useState}from'react';
import{CareerPathCard}from'../components/CareerPathVisuals';
import{careerCategories,careerPaths,type CareerCategory}from'../data/careerPaths';
import'../styles/career-paths.css';
export function CareerPathExplorerPage(){const[category,setCategory]=useState<'all'|CareerCategory>('all');const visible=useMemo(()=>category==='all'?careerPaths:careerPaths.filter(path=>path.category===category),[category]);return <div className="career-experience"><header className="career-hero"><div className="career-hero-orbit" aria-hidden="true"><i/><i/><i/></div><div><p className="career-eyebrow">JPAC creative futures</p><h1>Design Your Creative Journey</h1><p>Explore fourteen artistic, entertainment, media, production, business, and education pathways. Career milestones will recognize major verified achievements from the existing JPAC learning experience.</p><div className="career-hero-stats"><span><strong>14</strong> creative paths</span><span><strong>5</strong> major milestones each</span><span><strong>1</strong> protected learning engine</span></div></div></header><section className="career-explorer-intro" aria-labelledby="career-explorer-title"><div><p className="career-eyebrow">Career Path Explorer</p><h2 id="career-explorer-title">Find the future that sounds like you</h2></div><p>Available paths glow brightest. Future journeys stay visible so you can see what JPAC is building without mistaking a course shell for ready curriculum.</p></section><div className="career-category-filters" role="group" aria-label="Filter career paths by category">{careerCategories.map(item=><button type="button" key={item.id} aria-pressed={category===item.id} onClick={()=>setCategory(item.id)}>{item.label}</button>)}</div><p className="career-sr-only" aria-live="polite">Showing {visible.length} career paths</p><section className="career-card-grid" aria-label="JPAC career path catalog">{visible.map(path=><CareerPathCard path={path} key={path.slug}/>)}</section></div>}
